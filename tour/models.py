from django.db import models

from client.models import Client


# class Tour(models.Model):
#     owner = models.ForeignKey(Client, related_name='tours', on_delete=models.CASCADE)
    # price = models.DecimalField()
